# 규제 준수 발표 초안 — 문장별 사실 판정표

> Liam 초안(슬라이드 1~10, 2026-09-03)의 **문장 하나하나**를 지금 레포 기준으로 확인한 결과다.
> 경영진 앞에 나가는 자료이므로 "있다고 적었는데 없는 것"을 걸러내는 것이 이 표의 목적이다.
>
> - **구현됨** — 코드에 있고 동작을 확인했다. 파일:줄 또는 화면으로 근거를 댄다.
> - **부분** — 뼈대는 있으나 켜져 있지 않거나 일부만 된다. 빠진 것을 한 줄로 적었다.
> - **계획** — 아직 없다. 슬라이드 문안을 고쳤다.
>
> 판정 일자 2026-09-03 · 판정자 mai-family/docs
> 규제 이름은 **초안에 있는 것까지만** 썼다. 새로 늘리지 않았고, 규제 해석도 새로 만들지 않았다.

## 0. 집계

| 판정 | 건수 |
| --- | --- |
| 구현됨 | 12 |
| 부분 | 17 |
| 계획 | 8 |
| **합계** | **37** |

### 확인 범위 — 눌러 본 것과 코드로만 본 것

| 구분 | 제품 | 어떻게 |
| --- | --- | --- |
| **화면까지 눌러 봄** | MAGE · MESH · MAGMA · MAGNET | 스테이지·로컬에 붙어 로그인하고 화면을 찍었다 |
| **실행해 봄(CLI)** | MOJO | `mojo secret-scan` 을 실제로 돌려 결과를 찍었다 |
| **코드로만 봄** | MAGIC · MERLIN · MYSTIC · MARK · MANA | 띄우지 못했다(§4 미확보 목록) — 파일:줄 근거만 |

접속 실측(2026-09-03): MAGE `:8207` · MESH `:4321` · MAGMA `:3000` · MAGNET 로컬 `:5183`(웹) + `:8080`(API).
MAGNET 은 스테이지 컨테이너가 없어 로컬에서 띄웠다. 계정은 `maiadmin` 이며 비밀번호는 이 문서에 적지 않는다.

---

## 1. MAGE — 모든 AI의 안전한 지식 관문

| # | 초안 문장 | 판정 | 근거 / 빠진 것 |
| --- | --- | --- | --- |
| 1-1 | 사내의 모든 AI 호출이 반드시 거쳐 가야 하는 단일 게이트웨이(vLLM 프로세스 엔진) | **부분** | 관문은 있다(`MAGE/backend/main.py`). 다만 **vLLM 은 관문이 아니라 관문 뒤의 추론 엔진**이다(`MAGE/infra/docker-compose.app.yml:160` `mage-vllm`). 또 "반드시"가 기술로 강제되지 않는다 — MOJO 의 기본 OpenAI 주소가 외부값이라 설정으로 바뀔 수 있다(`MOJO/packages/core/src/core/openaiContentGenerator/constants.ts:4`) |
| 1-2 | 모든 트래픽이 모이므로 외부 AI 서비스로의 무단 전송을 원천 차단 | **부분** | 관문을 거치는 호출은 막힌다. 그러나 신원 헤더 부착이 제품마다 갈린다 — `mai-suite/contract/reports/conformance.md` 경고문: MOJO 는 anthropic 경로에 0건, MAGIC 은 `/v1/messages` 한 곳, MYSTIC 은 확장의 직접 호출 경로에 미부착. "원천"은 과하다 |
| 1-3 | DLP 필터링과 전량 감사 로그 기록이 내장 | **부분** | 코드는 있다 — `backend/main.py:71` DLP 미들웨어 등록, `main.py:84` 요청 가드, `backend/security/audit_trail.py:188` `emit_audit`, `backend/security/kill_switch.py`. 화면(관리 패널 컴플라이언스 체크리스트)에서 PII 마스킹·ACL·킬 스위치·프롬프트 인젝션 가드는 ✓ 인데 **감사 로그 기록만 ✗(0건)** 이다 — 스테이지에 기록이 쌓이지 않았다 |
| 1-4 | 별도 작업 없이 '전산자료 반출 통제'·'반기별 정보보호 이행 평가' 요건을 완벽하게 충족 | **계획** | 반기별 이행 평가 산출물을 만드는 기능을 찾지 못했다(`backend/reporting/synthetic.py` 는 합성 리포트 생성기다). "완벽하게 충족"의 근거가 없다 |

**고친 문안**

- 1-1 → "MAGE 는 사내 AI 호출이 모이는 단일 관문입니다. 추론은 사내 서버에서 처리되고, 제품들은 이 관문을 통해 사내 모델에 연결됩니다."
- 1-2 → "관문을 거치는 호출에서는 외부 전송이 차단됩니다. 관문을 우회할 수 있는 자리가 남아 있는지는 제품별로 점검하고 있습니다."
- 1-3 → "정보 유출 방지 필터와 감사 기록이 관문에 내장돼 있습니다. 항목별로 켜져 있는지는 관리 패널에서 바로 확인합니다."
- 1-4 → "반출 통제와 이행 평가에 필요한 기록을 관문 한 곳에 모읍니다. 규정 충족 여부는 감사 부서 검토를 거쳐 판정합니다."

---

## 2. MAGIC — 현업의 자율성을 보장하는 데스크톱 비서

| # | 초안 문장 | 판정 | 근거 / 빠진 것 |
| --- | --- | --- | --- |
| 2-1 | 임직원이 일상 업무에서 가장 먼저 만나는 데스크톱 AI 도우미 | **구현됨** | 데스크톱 앱 실재(`MAGIC/ui/desktop`, Tauri 셸, `package.json` 기준 1.38.0) |
| 2-2 | 회사가 승인한 안전한 환경 내에서 작동 | **부분** | 신원 헤더가 붙고(`MAGIC/crates/goose/src/providers/anthropic.rs:71`) 감사도 보낸다(`crates/goose-server/src/routes/mage_audit.rs:57`). 다만 부착이 `/v1/messages` 한 경로에 한정된다(conformance.md) |
| 2-3 | 현업 직원이 '시민개발(Tier 1)' 수준의 자율적 개발을 수행 | **계획** | 시민개발·개발 등급(Tier) 개념이 레포에 없다(전체 grep 0건) |
| 2-4 | 이 도구를 쓰는 것 자체가 사내 'AI 이용 수칙'을 기술적으로 준수하는 결과 | **계획** | 'AI 이용 수칙' 문서가 레포에 없다. 수칙이 없으면 준수 여부를 말할 수 없다 |

**고친 문안**

- 2-3 → "현업 직원이 문서·표 작업을 대화로 처리합니다. 개발 등급 체계와 어떻게 잇는지는 사내 AI 이용 수칙이 나온 뒤 정합니다."
- 2-4 → "회사가 정한 경로로만 AI 를 쓰게 되므로, 개인이 규정을 외워서 지킬 일이 줄어듭니다."

---

## 3. MOJO — 보안이 내재화된 터미널 코딩 CLI

| # | 초안 문장 | 판정 | 근거 / 빠진 것 |
| --- | --- | --- | --- |
| 3-1 | 개발자가 터미널에서 AI 를 활용하는 도구 | **구현됨** | `node packages/cli/dist/index.js --version` → `0.19.2`, `--help` 로 하위 명령 확인 |
| 3-2 | 비밀번호·API 키 같은 민감 자격증명을 철저히 봉인 | **구현됨** | `mojo secret-scan` 을 실제로 돌렸다 — 9건 탐지/10개 파일, 값은 `apiK************(len:20)` 로 **마스킹**되고 지문(sha256)만 남는다. 예외는 관리자 정책 파일(`.mai-policy.yml`)로만 허용된다 |
| 3-3 | 모든 요청을 사내 게이트웨이로만 향하도록 강제 | **부분** | 신원 헤더 모듈(`packages/core/src/providers/mai-identity-headers.ts`)과 감사 싱크(`packages/core/src/telemetry/mage-audit-sink.ts`)는 있다. 그러나 기본 OpenAI 주소가 외부값이고(`constants.ts:4`) anthropic 경로엔 헤더가 안 붙는다(conformance.md) — 설정으로 강제하는 것이지 코드가 막는 것이 아니다 |
| 3-4 | AI 가 만든 코드의 출처(provenance)를 안전하게 기록 | **계획** | 레포의 `provenance` 는 **MCP 서버 설정의 출처 범위**를 뜻한다(`packages/cli/src/config/settings.ts:365`). AI 생성 코드의 출처를 남기는 기능이 아니다 |

**고친 문안**

- 3-3 → "요청이 사내 게이트웨이로만 나가도록 설정으로 고정하고, 나갈 때마다 누가 무엇을 물었는지 신원을 붙입니다."
- 3-4 → "AI 가 손댄 변경은 조각 단위로 남고, 게이트웨이 기록과 함께 추적됩니다."

---

## 4. MERLIN — 인간이 통제하는 AI 코딩 위저드

| # | 초안 문장 | 판정 | 근거 / 빠진 것 |
| --- | --- | --- | --- |
| 4-1 | 사내 자체 개발한 Eclipse IDE 기반 | **구현됨** | OSGi 플러그인 4개 모듈(`com.proframe.mojo.core/ui/feature/updatesite`), JDK 1.6 · Eclipse 3.7 |
| 4-2 | AI 제안 코드는 개발자가 diff 를 확인하고 수락해야 함 | **구현됨** | `com.proframe.mojo.ui/src/.../views/PendingEditsView.java:109` 수락 버튼, `diff/StagedCompareInput.java:47` hunk 좌우 표시, `views/DiffHistoryView.java:199` 수락 hunk 수/전체 hunk 수 이력 |
| 4-3 | 필수 리뷰 과정을 거쳐야만 병합(Merge)이 가능 | **부분** | **소관이 다르다.** MERLIN 에는 머지 게이트가 없다. 그 통제는 MAGMA 에 있다(`MAGMA/server/policy/src/lib/mergeGate.js`) |
| 4-4 | 금융분야 AI 가이드라인의 보조수단성 원칙과 프로그램 이관 통제를 시스템적으로 강제 | **부분** | 사람이 수락해야 반영되는 구조는 제품에 실재한다. 다만 '이관 통제'는 MAGMA 몫이고, 규정 충족 판정은 별개다 |

**고친 문안**

- 4-3 → "MERLIN 은 조각 단위 수락과 그 이력을 남깁니다. 병합 승인은 MAGMA 가 맡습니다."
- 4-4 → "AI 가 짠 코드는 사람이 조각마다 확인하고 수락해야 들어갑니다. 누가 무엇을 수락했는지가 이력으로 남습니다."

---

## 5. MYSTIC — 데이터 노출 없는 대시보드 챗봇

| # | 초안 문장 | 판정 | 근거 / 빠진 것 |
| --- | --- | --- | --- |
| 5-1 | Tableau 대시보드와 연동된 대화형 챗봇 | **구현됨** | Chrome 확장 + FastAPI 백엔드 + Tableau MCP(`MYSTIC/server/src/`) |
| 5-2 | 사용자의 행 수준 권한(RLS)을 그대로 상속 | **구현됨** | `server/src/rls.py` — 공용 서비스 계정(env PAT) 폴백이 **기본 차단**이고(`RLS_ALLOW_ENV_CREDENTIAL_FALLBACK` 기본 `false`), 우회 시도는 `rls_bypass_attempt` 로 MAGE 감사에 전송된다 |
| 5-3 | 원천 DB 에 직접 접근하는 대신 대시보드의 집계값에만 질의 | **부분** | 앞부분은 맞다 — 질의는 Tableau **게시 데이터소스**로만 간다(`server/src/tableau_rest_client.py:12-14`, VizQL Data Service). 그러나 "집계값에만"은 아니다. 데이터소스 질의는 행도 돌려줄 수 있고, 보이는 범위는 RLS 가 정한다 |
| 5-4 | 고객 실데이터가 AI 테스트·질의에 무단 사용되는 것을 막아 '실데이터 테스트 금지' 규제를 준수 | **부분** | PII 필터는 있다(`server/src/pii_filter.py`). 그러나 테스트 데이터 정책을 강제하는 장치는 확인되지 않았다 |

**고친 문안**

- 5-3 → "원천 데이터베이스에 직접 붙지 않고 Tableau 게시 데이터소스에만 물어봅니다. 볼 수 있는 범위는 본인 권한이 정합니다."
- 5-4 → "질의와 답변에서 민감 정보를 걸러 냅니다. 실데이터 테스트 금지 요건과의 대응은 데이터 정책과 함께 맞춥니다."

---

## 6. MARK — 온라인 학습 커널

| # | 초안 문장 | 판정 | 근거 / 빠진 것 |
| --- | --- | --- | --- |
| 6-1 | 사내 AI 모델의 신뢰성을 높이기 위한 학습 전용 '환경' | **부분** | MARK 는 환경이 아니라 **파이썬 라이브러리**다 — `src/adaptive_learner/` 6개 모듈(learner·detectors·replay·context·mcp_server·`__init__`). LLM 재학습이 아니라 데이터 규칙 변화(concept drift) 온라인 학습이다 |
| 6-2 | DRB 를 통과한 '가명 데이터'에 한해서만 학습을 허용하는 데이터 가드가 작동 | **계획** | DRB·가명·데이터 가드 어느 것도 레포에 없다(전체 grep 0건). **이 문장은 그대로 쓰면 안 된다** |
| 6-3 | 사내 AI 모델의 합법적 지위를 유지하고 규제 신뢰성 원칙을 지켜냄 | **계획** | 6-2 가 없으므로 성립하지 않는다 |

**고친 문안**

- 6-1 → "MARK 는 데이터의 규칙이 바뀌는 것을 스스로 알아채는 학습 부품입니다."
- 6-2 → "어떤 데이터로 학습할지 심의를 거치게 하는 장치는 아직 만들지 않았습니다. 다음 단계 과제입니다."
- 6-3 → (삭제하고 6-2 의 계획 표기로 대체)

---

## 7. MANA — 스킬 아카이브

| # | 초안 문장 | 판정 | 근거 / 빠진 것 |
| --- | --- | --- | --- |
| 7-1 | 임직원이 만든 에이전트 스킬·프롬프트를 모아 공유하는 저장소 | **구현됨** | 에이전트 141 · 스킬 18 + 마켓플레이스 웹앱 + 관리자 콘솔(`0104564`) |
| 7-2 | 검역과 서명(검수) 체계를 거친 스킬만 공식 등재 | **부분** | 검수 게이트는 실재한다 — 외부 저작 스킬은 `skill_review.mark_pending()` 으로 검수 대기가 되고(`MAGE/backend/routers/graph_mcp_api.py:183`), 승인 전에는 노출되지 않는다(`:277` `is_approved`). 다만 **서명(signature) 체계는 없다**. 반입 검역은 MAGMA 소관이다(`MAGMA/server/policy/src/lib/quarantine.js`) |
| 7-3 | 외부 패키지 반입 통제 규제를 프롬프트·AI 스킬 영역까지 빈틈없이 확장 | **부분** | 검수 게이트까지는 맞다. "빈틈없이"의 근거는 없다. 덧붙일 사실이 있다 — MANA 에는 에이전트 거버넌스 체계가 따로 있다(소유·수명주기·권한 인벤토리 + `scripts/agent-audit.sh` 검사기 + MAGE 중앙 감사 전송, 커밋 `51c8c52`) |

**고친 문안**

- 7-2 → "밖에서 만든 스킬은 등록해도 바로 보이지 않습니다. 관리자 검수를 통과해야 사내에 노출됩니다."
- 7-3 → "코드에만 적용하던 반입 통제를 스킬과 프롬프트에도 같은 방식으로 겁니다. 누가 만든 스킬인지, 아직 쓰는 스킬인지도 함께 관리합니다."

---

## 8. MESH — 지식·문서 허브

| # | 초안 문장 | 판정 | 근거 / 빠진 것 |
| --- | --- | --- | --- |
| 8-1 | 사내 노션처럼 쓰이는 지식·문서 허브 | **구현됨** | 스테이지 `:4321` 로그인·워크스페이스 확인. Next.js 15 + BlockNote |
| 8-2 | 모든 AI 관련 정책·지침·이행 평가 증적을 모아두는 중앙 허브 | **부분** | 문서 허브와 **위변조 감지 감사 기록**은 실제로 돈다 — 화면에서 이벤트 56건, 체인 해시 열, 해시 불일치 경보까지 확인했다. 다만 'AI 정책 증적'이라는 **내용은 아직 채워지지 않았다**. 그릇은 있고 내용이 없다 |
| 8-3 | AI 기본법 대응 — '고영향 AI' 판단 기록·'생성물 표시 정책'을 문서화·보존해 법적 투명성 의무를 입증 | **계획** | 고영향 AI·생성물 표시 관련 코드·문서·서식이 레포에 없다(grep 0건) |

**고친 문안**

- 8-2 → "정책과 지침을 한곳에 두고, 누가 언제 무엇을 고쳤는지 위변조 없이 남깁니다."
- 8-3 → "AI 관련 판단 기록을 어떤 서식으로 남길지는 다음 단계입니다. 기록을 위변조 없이 보존하는 바탕은 이미 돌고 있습니다."

---

## 9. MAGNET — 대화·협업 메신저

| # | 초안 문장 | 판정 | 근거 / 빠진 것 |
| --- | --- | --- | --- |
| 9-1 | 사내 전용 협업 메신저 | **구현됨** | 로컬 기동·로그인 화면 확인(`:5183` 웹, `:8080` API). **주의**: 카탈로그에 남은 'Rocket.Chat 리브랜딩' 서술은 옛 정보다 — `MAGNET/README.md:3` 은 "밑바닥부터 새로 만든 제품이며 어떤 오픈소스 메신저의 파생판도 아니다" |
| 9-2 | 외부 클라우드·개인 메신저를 쓸 유혹을 없앰(폐쇄망 전용) | **구현됨** | `README.md:69` — 산출물이 CDN·웹폰트·원격 이미지를 하나도 참조하지 않는다 |
| 9-3 | 개발 수요를 접수하는 단일 창구 역할 | **계획** | 수요 접수 기능·워크플로가 레포에 없다(grep 0건) |
| 9-4 | 코드 조각·내부 데이터를 채널 안에서만 공유하게 유도해 무단 반출 위험을 자연스럽게 소멸 | **부분** | AI 질의는 MAGE 테넌트 게이트웨이로만 가고 신원이 붙는다(`plugins/magnet-ai/src/gateway.ts:68-78`). 다만 채널 밖 반출을 막는 장치는 아니다 — "소멸"은 과하다 |

**고친 문안**

- 9-3 → "협업이 사내 채널 안에서 끝나게 합니다. 개발 수요 접수 창구로 쓰는 것은 다음 단계 계획입니다."
- 9-4 → "대화도 파일도 사내에 머무릅니다. 밖으로 나갈 이유를 줄이는 것이 이 제품이 하는 일입니다."

---

## 10. MAGMA — Git · CI/CD · 아티팩트 플랫폼

| # | 초안 문장 | 판정 | 근거 / 빠진 것 |
| --- | --- | --- | --- |
| 10-1 | 사설 미러 저장소로 외부 오픈소스의 라이선스·취약점·악성코드를 스캔 | **부분** | 라이선스(`server/policy/src/ci/licenseScan.js`)·시크릿(`secretScan.js`)·취약 패턴 정적 검사(`sastScan.js`)는 있다. **악성코드 스캔은 없다.** 취약점은 스캐너가 아니라 SBOM 역조회로 매칭한다(`src/lib/sbomStore.js`). '사설 미러 저장소'도 확인되지 않았다 |
| 10-2 | SBOM 을 자동으로 생성 | **구현됨** | `server/policy/src/lib/sbom.js` — SPDX 2.3 / CycloneDX 1.5 산출, 조회 API(`src/server.js:83`), CLI(`src/cli/sbomGen.js`) |
| 10-3 | MR 승인과 CI/CD 를 거치지 않으면 운영 환경으로 절대 이관 불가 | **부분** | 코드는 강하다 — 보호 브랜치·필수 승인·새 커밋 시 승인 무효화·셀프 승인 배제·C1 경로 CODEOWNERS·`magma/policy-gate` 상태 체크로 머지 버튼 잠금(`server/policy/README.md`). 그러나 **policy 서비스가 스테이지에 떠 있지 않다**(컨테이너 없음) 그리고 웹훅 등록이 배포 시 수동 작업으로 남아 있다 |
| 10-4 | 개발·운영 분리와 보안성 검증을 명시한 전자금융감독규정을 기술적으로 완벽히 강제 | **부분** | 직무분리(`src/lib/deploymentSod.js`)와 불변 이력(`src/lib/auditLedger.js`)이 있다. 다만 미가동이므로 "완벽히 강제"는 지금 말할 수 없다 |

**고친 문안**

- 10-1 → "외부에서 들여온 코드는 반입 검역을 거치고, 라이선스·시크릿·취약 패턴을 자동으로 검사합니다."
- 10-3 → "승인 없이는 머지 버튼이 잠깁니다. 본인 승인과 본인 머지는 정족수에서 빠집니다."
- 10-4 → "만든 사람과 올리는 사람을 분리하고, 그 이력을 고칠 수 없게 남깁니다."

---

## 11. 슬라이드에 계획으로 표시할 항목 (8건)

발표에서 "됩니다"로 말하면 안 되는 것들이다. 덱에서는 **계획 배지**를 달았다.

| 슬라이드 | 항목 |
| --- | --- |
| 1 | 반기별 정보보호 이행 평가 산출 |
| 2 | 시민개발(Tier 1) 등급 체계 |
| 2 | 사내 AI 이용 수칙 |
| 3 | AI 생성 코드의 출처 기록 |
| 6 | 학습 데이터 심의(DRB) 연동 |
| 6 | 가명 데이터 한정 학습 가드 |
| 8 | 고영향 AI 판단 기록·생성물 표시 정책 서식 |
| 9 | 개발 수요 접수 단일 창구 |

## 12. 사실관계가 어긋난 세 곳 (문장을 반드시 고쳐야 함)

1. **MAGE = vLLM 프로세스 엔진** (1-1) — vLLM 은 관문이 아니라 관문 뒤의 추론 엔진이다.
2. **MERLIN 이 병합을 막는다** (4-3) — 머지 게이트는 MAGMA 에 있다. 제품 소관이 뒤바뀌었다.
3. **MARK 의 DRB 가명 데이터 가드** (6-2) — 코드에 흔적이 없다. 그대로 발표하면 그 자리에서 사고가 난다.

덧붙여, 카탈로그에 남은 **MAGNET = Rocket.Chat 리브랜딩** 서술도 옛 정보다(이번 건 범위 밖이라 고치지 않았고 별도 정리 대상으로만 적어 둔다).
